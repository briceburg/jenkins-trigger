---
credentials:
  system:
    domainCredentials:
    - credentials:
      - string:
          id: "slack-token"
          scope: GLOBAL
          description: "slack token"
          secret: ${example_secret}
